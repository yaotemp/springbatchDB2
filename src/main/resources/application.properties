# H2 datasource (Spring Batch metadata)
spring.datasource.h2.url=jdbc:h2:file:./data/batch-metadata;DB_CLOSE_DELAY=-1
spring.datasource.h2.username=sa
spring.datasource.h2.password=
spring.datasource.h2.driver-class-name=org.h2.Driver

# DB2 datasource (business data) - currently pointing to MySQL for development
app.datasource.db2.url=jdbc:mysql://localhost:3306/springbatch?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
app.datasource.db2.username=root
app.datasource.db2.password=admin
app.datasource.db2.driver-class-name=com.mysql.cj.jdbc.Driver

# Spring Batch metadata table initialization
spring.batch.jdbc.initialize-schema=always

# Disable automatic job execution
spring.batch.job.enabled=false

# Output file path
output.csv.path=./output/output.csv